[user]
	name = hidalz
	email = 46967257+hidalz@users.noreply.github.com

[init]
	defaultBranch = main

[alias]
    co = checkout
    br = branch
    ci = commit
    st = status

[core]
      pager = delta
      editor = nvim

[interactive]
      diffFilter = delta --color-only

[delta]
      navigate = true    # use n and N to move between diff sections
      light = false      # set to true if you're in a terminal w/ a light background color (e.g. the default macOS terminal)

[merge]
      conflictstyle = diff3

[diff]
      colorMoved = default
